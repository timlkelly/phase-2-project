<h1><%= @user.first_name %>'s Profile</h1>


<div class="">

  <h2>Saved locations</h2>

  <% if @user.saved_locations.length > 0 %>
    <% @user.saved_locations.each do |saved_location| %>

    <div class="location">
      <p> <a href="/locations/<%= saved_location.id %>"><%= saved_location.name %></a> </p>
      <p>Lat: <%= saved_location.location.latitude %></p>
      <p>Long: <%= saved_location.location.longitude %></p>


<!--       <div class="saved-conditions-container">
        <form method="get" action="/locations/<%= saved_location.id %>">
          <input type="hidden" name="latitude" value="<%= saved_location.location.latitude %>">
          <input type="hidden" name="longitude" value="<%= saved_location.location.longitude %>">
          <input type="submit" value="Check current conditions" class="saved-check-conditions">
        </form>
      </div> -->

    </div>

    <% end %>
  <% end %>

</div>

<div>
  <h2>Add a location</h2>

  <!-- Placeholder for a partial form to add a location -->

</div>

